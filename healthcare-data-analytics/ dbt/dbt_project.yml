name: 'healthcare_dbt'
version: '1.0.0'
config-version: 2

profile: 'jaffle_shop_1'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  healthcare_dbt:
    staging:
      +materialized: view

    marts:
      +materialized: table
